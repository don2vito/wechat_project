# 【Prompt】氛围编程生图提示词（基于 Trea）



[TOC]



`/ spec`

## 第一步 使用 Python 绘图库复现图片

使用 Python 语言，基于提供的**{数据表图片}** 和**{目标效果图}** ，开发一个可复用的数据可视化脚本。具体要求如下：

1. 技术实现：
- 使用pandas库实现数据的读取、清洗与转换操作，确保数据处理流程更加高效和可维护，支持主流的 excel、csv、txt 等常用数据格式，提高代码的数据适应性和可维护性。
- 使用 matplotlib 或其他适合的 Python 数据可视化库，精确复现效果图的视觉样式，包括但不限于颜色方案、布局结构、字体样式、数据展示方式等视觉元素
2. 功能要求：

- 实现数据与图表的动态关联，确保后续数据表更新时，脚本能够自动适应并生成格式一致的图表，实现像素级视觉匹配

- 图表需包含完整的标题、坐标轴标签、数据标签、环比箭头等所有视觉元素

- 确保图表中数据展示的准确性和可读性

- 对代码文件进行全面注释，包括：模块功能说明、核心函数用途、关键变量含义、算法实现原理及使用注意事项，确保其他开发者能在30分钟内理解代码整体架构和实现细节

- 数据格式标准化：所有数值数据统一采用带千分位分隔符的整数格式（例如：1000应显示为1,000），不保留任何小数位

- 确保图片中的数字和字体的大小，能够保证视觉清晰、阅读舒适的尺寸

- 对 Python 代码内容，添加上详细的注释，以方便学习和使用

- 确保中文字体显示正常，无乱码或排版问题

3. 交付物：

- 完整的 Python 实现代码，包含详细的注释说明（保存为 .py 文件）

- 最终生成的 PNG 格式图片文件，需与目标效果图在视觉上保持高度一致

- 所有交付文件需保存至**{指定文件夹}**，文件名保存为**{代码文件名}**

- 创建一份详细的使用说明文档，文档命名为“使用说明.md”。该文档应包含产品/系统的基本介绍、功能说明、操作步骤、常见问题解答及注意事项等核心内容。文档需采用Markdown格式编写，结构清晰，语言简洁易懂，适合不同技术水平的用户阅读。内容应包括但不限于：产品概述、安装/配置指南、主要功能模块说明、详细操作流程（含截图或示例）、故障排除方法等。确保文档内容准确无误，逻辑连贯，并符合行业通用的技术文档规范。

4. 代码规范：

- 代码需遵循 PEP 8 规范，结构清晰，命名规范

- 关键步骤需提供详细注释，说明实现思路和方法

- 需包含适当的异常处理机制，确保脚本的健壮性

5. 可维护性要求：

- 代码需设计为模块化结构，便于后续维护和功能扩展

- 关键参数（如图表尺寸、颜色配置等）应集中管理，便于统一调整

- 提供简要的使用说明，指导如何更新数据表并重新生成图表



## 第二步 进行数据测试

请生成一个新的测试数据表（CSV格式文件），用于验证图片生成功能在不同数据条件下的表现。该测试数据表必须满足以下要求：1) 数据结构与 **{代码文件}** 中定义的图表数据结构存在显著差异；2) **{……}**。 

 使用  **{代码文件}**  中实现的图片生成逻辑处理上述新创建的测试数据表，系统性验证代码在以下方面的表现： 
 1. 不同数据输入情况下的程序执行稳定性与完整性 
 2. 生成图表的视觉渲染质量，包括但不限于色彩准确性、元素清晰度和布局合理性 
 3. 数据可视化的准确性，确保图表能够准确反映原始数据特征 
 4. 异常数据处理能力，特别是针对环比为负数的特殊情况 

 测试过程中需重点检查以下内容： 
 - 图表元素的完整性（标题、坐标轴、图例、数据标签等是否完整显示） 
 - 数据与图表元素的映射正确性（数值、趋势、环比关系是否准确呈现） 
 - 代码对边界条件和异常数据的处理能力 
 - 生成图片的格式规范性和兼容性 

 请记录测试过程中发现的所有异常情况、渲染问题或数据可视化偏差，并对测试结果进行分析总结。



## 第三步 生成操作手册

针对项目（**{项目文件夹路径}**），创建一份详细的操作手册，命名为"操作手册.md"。该手册需包含以下核心内容：项目概述、环境配置要求（Python 版本、依赖库清单及安装方法）、数据表格式规范与存放位置说明、图片生成功能的详细操作步骤（含参数配置说明）、常见问题排查与解决方案、输出图片的保存路径及格式说明。确保手册内容清晰易懂，步骤详细可操作，适用于技术与非技术人员使用。



## 第四步 使用 Streamlit 实现代码功能（可交互）

 使用 Streamlit 框架为**{项目文件夹路径}**项目开发一个功能完整的交互式数据可视化应用，具体实现以下需求：

1. **数据导入功能**：在应用界面顶部实现一个直观的文件导入按钮，支持用户上传本地数据表文件。需兼容多种常见格式，包括但不限于 Excel(.xlsx, .xls)、CSV(.csv)和文本文件(.txt)。实现文件类型验证、错误处理机制，并在上传成功后显示文件基本信息（如文件名、大小、数据量），可展开的数据预览（显示前 5 行）。

2. **数据可视化功能**：基于导入的数据，自动图片。图片的坐标轴需有明确标签，图表标题应反映数据主题。确保图表渲染高效且支持基本交互（如悬停显示详细数据）。支持自定义图表标题。确保中文字体显示正常，无乱码或排版问题。

3. **图表颜色配置功能**：每个区域可独立选择颜色（颜色选择器）。

4. **图片导出功能**：在图表下方实现一个导出按钮，允许用户将生成的环比箭头柱状图以 PNG 格式保存到本地指定文件夹。需提供文件保存路径选择功能，确保导出图片质量清晰，分辨率可选：150/300/600 DPI（默认 300 DPI），支持多种导出格式：PNG、JPG、PDF、SVG，并在导出完成后显示成功提示，自动生成带时间戳的文件名。

应用整体设计应遵循 Streamlit 最佳实践，确保界面简洁直观、响应迅速，并具备基本的错误处理和用户引导功能。



## 第五步 使用 Gradio 实现代码功能（可交互）

使用 Gradio 框架为**{项目文件夹路径}**项目开发一个功能完整、用户友好的交互式数据可视化应用，具体实现以下详细需求：

1. **数据导入功能**：

- 在应用界面顶部实现一个直观的文件导入按钮，支持用户上传本地数据表文件

- 兼容多种常见格式：Excel(.xlsx, .xls)、CSV(.csv)和文本文件(.txt)

- 实现严格的文件类型验证机制，对不支持的文件格式提供明确错误提示

- 添加文件大小限制（建议不超过 10 MB）及相应错误处理

- 上传成功后，在界面指定区域显示文件基本信息：文件名、文件大小、数据行数、列数及数据预览（前 5 行）

- 实现数据加载状态指示，处理大型文件时显示加载进度

2. **颜色配置功能**：

- 为可视化图表的不同数据区域提供独立的颜色选择器

- 每个颜色选择器应包含预设颜色选项和自定义颜色输入功能

- 颜色变更后应实时更新图表显示，无需重新导入数据

- 提供"重置为默认颜色"功能

3. **数据可视化功能**：

- 基于导入的数据自动生成高质量图片
- 坐标轴需有明确标签，包含单位（如适用）
- 图表标题应自动从数据中提取或允许用户自定义
- 确保图表渲染高效，大数据集下加载时间不超过 3 秒
- 支持交互功能：悬停显示详细数据（包括具体数值和环比百分比）、缩放和平移
- 图表应具备响应式设计，适应不同屏幕尺寸

4. **图片导出功能**：

- 在图表下方实现醒目的导出按钮，允许用户将生成的环比箭头柱状图以 PNG 格式保存到本地

- 提供文件保存路径选择对话框

- 确保导出图片质量清晰，分辨率不低于 300 dpi

- 导出文件默认命名格式："Gradio 生成图片_YYYYMMDD_HHMMSS.png"

- 导出完成后显示明确的成功提示，并提供"打开文件位置"选项

- 实现导出错误处理，对权限问题或存储问题提供友好提示

应用整体要求：

- 遵循 Gradio 最佳实践，采用模块化设计

- 界面布局合理，各功能区域划分清晰，操作流程直观

- 确保应用响应迅速，用户交互延迟不超过 500 ms

- 实现全面的错误处理机制，对数据格式错误、文件读取失败等异常情况提供明确指导

- 添加适当的用户引导和帮助信息

- 确保代码可维护性，包含必要的注释和文档

- 应用应能在主流浏览器（Chrome、Firefox、Edge）中正常运行



## 第六步 对已实现的代码功能打包成 exe 程序（带界面）

将**{项目文件夹路径}**项目打包为功能完整的 Windows 可执行(exe)程序，需满足以下详细技术要求和实现标准：

1. **数据导入功能实现规范**：

- 在应用主界面顶部设计并实现直观的文件导入按钮，按钮需有明确图标和"导入数据"文字标识

- 支持文件格式：Excel(.xlsx, .xls)、CSV(.csv)和文本文件(.txt)，需确保各格式文件的正确解析

- 实现严格的文件类型验证机制，对不支持格式提供明确错误提示

- 开发完善的错误处理逻辑，包括文件读取错误、格式错误、数据结构异常等场景的友好提示

- 上传成功后，在界面指定区域显示文件元信息：原始文件名、文件大小(MB)、数据记录条数、导入时间戳

2. **数据可视化功能技术要求**：

- 基于导入的结构化数据，自动分析并生成图片
- 图表标题需根据导入数据自动生成，准确反映数据主题和时间维度
- 实现高效的图表渲染引擎，确保在数据量较大(1000+ 数据点)时仍保持流畅操作
- 支持交互功能：鼠标悬停显示详细数据(数值、环比百分比、变化量)，支持图表缩放和平移

3. **图片导出功能技术规范**：

- 在图表下方位置实现"导出图片"按钮，样式与导入按钮保持视觉一致性

- 支持将生成的环比箭头柱状图以 PNG 格式导出，提供标准文件保存对话框供用户选择保存路径

- 图片质量要求：分辨率不低于 300 dpi，图像清晰无模糊，色彩还原准确

- 实现导出进度提示，完成后显示模态成功提示框，包含文件保存路径信息

- 确保导出图片包含完整图表元素，包括标题、坐标轴、图例和数据标签

4. **打包与部署要求**：

- 使用适合的 Python 打包工具(如 PyInstaller、cx_Freeze 等)将项目打包为单个可执行 exe 文件

- 确保打包后的程序可在 Windows 10 / 11 系统上独立运行，无需额外安装 Python 环境

- 程序启动时间控制在 5 秒以内，内存占用合理，无明显性能问题

- 生成的 exe 文件需包含应用图标，安装包大小控制在合理范围

- 提供基本的错误日志记录功能，便于排查运行时问题

5. **用户界面要求**：

- 整体设计简洁直观，符合现代 UI 设计规范，各功能区域布局合理

- 实现响应式界面，支持不同屏幕分辨率自适应显示

- 提供清晰的操作指引和帮助信息，降低用户学习成本

- 确保中文字体显示正常，无乱码或排版问题

最终交付物应包括可直接运行的 exe 程序、详细的打包配置说明文档以及必要的测试报告。